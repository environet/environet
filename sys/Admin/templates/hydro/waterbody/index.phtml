<?php include __DIR__ . '../../../layouts/before_app.phtml'; ?>
<div class="card">
    <div class="d-flex w-100">
		<div class="mr-auto form-group">
			<a href="/admin/hydro/waterbodies/add" class="btn btn-primary" >Add waterbody</a>
		</div>
		<div class="form-group">
			<form method="get">
				<input type="search" name="search" class="form-control" placeholder="<?=__('Search')?>" value="<?=htmlspecialchars(urldecode($searchString))?>">
			</form>
		</div>
	</div>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th><?=sortableColumn('Code', 'european_river_code');?></th>
                <th><?=sortableColumn('Cname', 'cname');?></th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php
				if (count($records) > 0):
					foreach ($records as $record):
			?>
                <tr>
                    <td><?=$record['european_river_code'] ?? null;?></td>
                    <td><?=$record['cname'] ?? null;?></td>
                    <td class="actions">
                        <a href="/admin/hydro/waterbodies/show?id=<?=$record['european_river_code'];?>"><i class="fas fa-eye text-muted"></i></a>
                        <a href="/admin/hydro/waterbodies/edit?id=<?=$record['european_river_code'];?>"><i class="fas fa-edit"></i></a>
                        <a href="/admin/hydro/waterbodies/delete?id=<?=$record['european_river_code'];?>" onclick="return confirm('Are you sure you want to delete this waterbody?')"><i class="fas fa-trash text-danger"></i></a>
                    </td>
                </tr>
			<?php
					endforeach;
				else:
			?>
					<tr>
						<td colspan="3">
							There is no record to show.
						</td>
					</tr>
			<?
				endif;
            ?>
        </tbody>
        <tfoot>
            <tr>
                <td colspan="4">
                    <div class="paginator">
                        <?php include __DIR__.'/../../parts/paginator.phtml';?>
                    </div>
                </td>
            </tr>
        </tfoot>
    </table>
</div>
<?php include __DIR__ . '../../../layouts/after_app.phtml'; ?>
