<form id="uploadDataForm" action="" method="POST" enctype="multipart/form-data">
	<div class="mb-3">
		<label for="mpoint" class="form-label"><?=__('CSV files');?></label>
		<input type="file" name="csv[]" class="form-control" multiple />
		<br />
		<small id="passwordHelpBlock" class="form-text text-muted">
			You can upload maximum <?=$maxFiles;?> files. <br />
			Maximal cumulate size of uploaded files: <?=$maxSize;?>
		</small>
	</div>
	<div class="mb-3">
		<div class="field">
			<label for="uploadTimezoneSelector" class="form-label">Timezone of dates in uploaded files</label>
			<select name="timezone_selector" id="uploadTimezoneSelector" class="form-select" required>
				<option value=""><?=__(' - Please select - ');?></option>
				<?php foreach($timezoneOptions as $timezoneOption) { ?>
					<option value="<?=$timezoneOption;?>" <?=($selectedTimezoneOption === $timezoneOption) ? 'selected' : '';?>>
						<?=$timezoneOption;?>
					</option>
				<?php } ?>
			</select>
		</div>
	</div>
	<input type="hidden" name="__csrf" value="<?=$csrf;?>"/>
	<input type="submit" class="btn btn-primary oneClickSubmit" data-processingvalue="Processing and validation is in progress..." value="Send">
</form>